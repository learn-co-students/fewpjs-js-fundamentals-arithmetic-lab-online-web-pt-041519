// Generate 62 as the result of multiplication
const num1 = 31;
const num2 = 2;
const multiply = num1 * num2;

// Generate random number greater than 0
const random = Math.ceil(Math.random() * 100);

// Generate modulus equal to 4
const mod = 68 % 8;

// Generate 20 as the result of a Math.max calculation
const max = Math.max(1, 3, 5, 20);
